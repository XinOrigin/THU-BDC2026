services:  
  app:
    image: bdc2026:latest
    pull_policy: never
    volumes:
      - ./data:/app/data
      - ./test/output:/app/output
      - ./temp:/app/temp
      - ./model:/app/model
      - .:/app/code  # 将当前目录挂载到容器的/app/code，方便调试和修改代码
    working_dir: /app
    ports:
      - "6006:6006"
    # 先临时替换命令，验证GPU是否能访问
    #command: /bin/bash /app/data/run.sh
    # 核心配置：指定NVIDIA运行时+暴露GPU
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    privileged: true
    mem_limit: 16g
    memswap_limit: 16g
    cpus: 10.0